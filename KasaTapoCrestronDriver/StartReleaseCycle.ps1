param(
	[string] $Version = '1.0.0'
)

Write-Host "Prepare release metadata for version $Version."
Write-Host 'Update the driver manifest, package metadata, and release notes as needed before tagging.'
